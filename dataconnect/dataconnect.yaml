specVersion: "v1"
serviceId: "wager"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wager-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
